{
  "catalog_version": 1,
  "definition_schema_version": 1,
  "tools": [
    {
      "annotations": {
        "destructiveHint": true,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Read/update allowlisted RepoPrompt app-wide preferences. Settings outside the allowlist are not exposed.\n\n**Operations**: `list` (catalog), `get` (read), `set` (write one key), `options` (candidate values for keys with `options_available: true`).\n\n**Selectors**: `get` accepts exactly one of `key`, `keys`, or `group`. `set` and `options` take one `key`.\n\n**Groups**: `ui` · `prompt_packaging` · `models` · `context_builder` · `mcp` · `code_maps` · `file_system` · `agent_mode`\n\n**Examples**:\n- `{\"op\":\"list\",\"group\":\"ui\"}`\n- `{\"op\":\"get\",\"keys\":[\"ui.appearance_mode\",\"ui.show_tooltips\"]}`\n- `{\"op\":\"get\",\"group\":\"file_system\"}`\n- `{\"op\":\"set\",\"key\":\"models.planning_model\",\"value\":null}`\n- `{\"op\":\"set\",\"key\":\"file_system.global_ignore_defaults\",\"value\":\"**/node_modules/\\n\"}`\n- `{\"op\":\"options\",\"key\":\"models.planning_model\",\"agent\":\"codexExec\"}`\n\nInvalid or out-of-range values are rejected with no partial apply. Model-raw settings accept custom identifiers beyond what `options` returns.",
      "name": "app_settings",
      "input_schema": {
        "properties": {
          "agent": {
            "description": "Filter options by CLI backend.",
            "type": "string"
          },
          "detailed": {
            "description": "Include descriptions and model metadata.",
            "type": "boolean"
          },
          "group": {
            "description": "Settings group.",
            "enum": [
              "ui",
              "prompt_packaging",
              "models",
              "context_builder",
              "mcp",
              "code_maps",
              "file_system",
              "agent_mode"
            ],
            "type": "string"
          },
          "key": {
            "description": "Allowlisted setting key (required for set/options).",
            "type": "string"
          },
          "keys": {
            "description": "Multiple keys (get only).",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "limit": {
            "description": "Maximum options returned (1–200).",
            "type": "integer"
          },
          "op": {
            "description": "Operation.",
            "enum": [
              "list",
              "get",
              "set",
              "options"
            ],
            "type": "string"
          },
          "value": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "integer"
              },
              {
                "type": "number"
              },
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ]
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "application"
      ],
      "scope": "application",
      "capability": "app_settings",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "list",
          "get",
          "set",
          "options"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "application"
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "List, inspect, and bind the canonical workspace context for this MCP connection. Bind by working_dirs (preferred) or context_id. This global tool never accepts an app window selector.",
      "name": "bind_context",
      "input_schema": {
        "properties": {
          "context_id": {
            "description": "For bind: canonical compose-tab context UUID",
            "type": "string"
          },
          "create_if_missing": {
            "description": "For bind with working_dirs: create a new workspace after approval if no exact or superset workspace matches",
            "type": "boolean"
          },
          "op": {
            "description": "Operation: 'list', 'status', or 'bind'",
            "enum": [
              "list",
              "status",
              "bind"
            ],
            "type": "string"
          },
          "tab_name": {
            "description": "Optional workspace name when creating via working_dirs + create_if_missing",
            "type": "string"
          },
          "working_dirs": {
            "description": "For bind: comma-separated absolute workspace root paths; exact match first, then repo_paths superset fallback",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "application"
      ],
      "scope": "application",
      "capability": "workspace_mutate",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "list",
          "status",
          "bind"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "application"
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": true,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Manage workspaces and compose-tab lifecycle across RepoPrompt windows.\n\n**This is the workspace inventory view.** `bind_context` remains the canonical API for per-window tab routing and context_id discovery. Legacy-compatible `list_tabs` and `select_tab` actions are restored for older clients, but new integrations should prefer `bind_context`.\n\nActions:\n• list         – Return known visible workspaces by default (id, name, repoPaths, showing window IDs, is_hidden)\n• switch       – Switch a window to a specified workspace\n• create       – Create a new workspace (optional folder_path)\n• hide         – Hide a workspace from default workspace lists without deleting it\n• unhide       – Restore a hidden workspace to default workspace lists\n• delete       – Delete a workspace permanently (optionally close window)\n• add_folder   – Add a folder to a workspace (defaults to active workspace)\n• remove_folder – Remove a folder from a workspace (defaults to active workspace)\n• list_tabs    – List compose tabs in one window (Legacy compatibility — prefer bind_context op=list)\n• select_tab   – Bind this connection to a compose tab (Legacy compatibility — prefer bind_context op=bind context_id=<id>)\n• create_tab   – Create a new compose tab in the background\n• close_tab    – Close a compose tab safely\n\nParameters:\n- action: \"list\" | \"switch\" | \"create\" | \"hide\" | \"unhide\" | \"delete\" | \"add_folder\" | \"remove_folder\" | \"list_tabs\" | \"select_tab\" | \"create_tab\" | \"close_tab\" (required)\n- workspace: string                             (required for 'switch', 'hide', 'unhide', 'delete'; optional for 'add_folder', 'remove_folder' - defaults to active workspace; UUID or name)\n- name: string                                  (required for 'create'; optional for 'create_tab')\n- folder_path: string                           (required for 'add_folder', 'remove_folder'; optional for 'create' to initialize with a root folder; absolute path)\n- tab: string                                   (required for 'select_tab'; optional for 'close_tab'; UUID or name)\n- mode: \"blank\" | \"fork\"                      (optional for 'create_tab'; default \"blank\")\n- source_tab: string                            (optional for 'create_tab' when mode=\"fork\"; UUID or name)\n- bind: boolean                                 (optional for 'create_tab'; default true)\n- focus: boolean                                (optional for 'select_tab' or 'create_tab'; if true, also switches the UI to show the tab)\n- allow_active: boolean                         (optional for 'close_tab'; default false)\n- window_id: integer                            (optional; target window, defaults to selected or only window)\n- open_in_new_window: boolean                   (optional for 'switch' or 'create'; when true, opens workspace in a new window and binds the connection to it)\n- switch_to_created: boolean                    (optional for 'create'; when true, switches to the newly created workspace)\n- close_window: boolean                         (optional for 'delete'; when true, switches away without saving, deletes the workspace, then requests window close)\n- include_hidden: boolean                       (optional; default false. For 'list', includes hidden workspaces. For name-based 'switch'/'delete', allows hidden matches. UUID lookup remains explicit and can resolve hidden workspaces.)\n\nHidden workspaces remain persisted/recoverable. Default 'list' and name-based 'switch'/'delete' exclude hidden workspaces unless include_hidden=true; 'hide'/'unhide' are non-destructive. Explicit UUID switch/delete can target hidden workspaces without unhiding them.\n\n**Relationship with bind_context:**\n- `manage_workspaces.list` returns workspace inventory: names, folder paths, and which windows show each workspace\n- `bind_context.list` returns per-window routing state: windows, active tabs, context_ids, and current binding\n- When the same workspace is open in multiple windows, compose tabs are shared — use `bind_context` to discover per-window context_ids\n\ncreate_tab defaults to bind=true and focus=false so automation can create isolated background tabs without stealing UI focus.\n\nIMPORTANT: The 'focus' parameter switches the visible tab in the UI, which can be disruptive to the user's workflow. Only set focus=true when the user explicitly requests to see or switch to a specific tab. For background operations, omit focus or set it to false. The 'close_tab' action refuses to close the last remaining tab, the active visible tab unless allow_active=true, or any tab with a live bound run.",
      "name": "manage_workspaces",
      "input_schema": {
        "properties": {
          "action": {
            "description": "Action to perform. Legacy compatibility: prefer bind_context for list_tabs/select_tab when building new integrations.",
            "enum": [
              "list",
              "switch",
              "create",
              "hide",
              "unhide",
              "delete",
              "add_folder",
              "remove_folder",
              "list_tabs",
              "select_tab",
              "create_tab",
              "close_tab"
            ],
            "type": "string"
          },
          "allow_active": {
            "description": "For 'close_tab': allow closing the currently active visible tab",
            "type": "boolean"
          },
          "bind": {
            "description": "For 'create_tab': if true, bind this MCP connection to the new tab (default true)",
            "type": "boolean"
          },
          "close_window": {
            "description": "For 'delete': when true, switches away without saving, deletes the workspace, then requests window close.",
            "type": "boolean"
          },
          "focus": {
            "description": "For 'select_tab' or 'create_tab': if true, also switches the UI to show the tab",
            "type": "boolean"
          },
          "folder_path": {
            "description": "Absolute folder path (required for 'add_folder', 'remove_folder'; optional for 'create' to initialize with a root folder)",
            "type": "string"
          },
          "include_hidden": {
            "description": "Default false. For list, includes hidden workspaces. For name-based switch/delete, allows hidden matches; UUID lookup remains explicit.",
            "type": "boolean"
          },
          "mode": {
            "description": "For 'create_tab': creation mode ('blank' or 'fork')",
            "type": "string"
          },
          "name": {
            "description": "Name for new workspace (required for 'create'; optional for 'create_tab')",
            "type": "string"
          },
          "open_in_new_window": {
            "description": "For 'switch' or 'create': when true, opens workspace in a new window and binds connection to it. Returns window_id in response.",
            "type": "boolean"
          },
          "source_tab": {
            "description": "For 'create_tab' with mode='fork': source compose tab UUID or name",
            "type": "string"
          },
          "switch_to_created": {
            "description": "For 'create': when true, switches to the newly created workspace in the target window.",
            "type": "boolean"
          },
          "tab": {
            "description": "Compose tab UUID or name (required for 'select_tab'; optional for 'close_tab')",
            "type": "string"
          },
          "window_id": {
            "description": "Optional window ID; defaults to selected or only window",
            "type": "integer"
          },
          "workspace": {
            "description": "Workspace UUID or name (required for 'switch', 'hide', 'unhide', 'delete'; optional for 'add_folder', 'remove_folder' - defaults to active workspace)",
            "type": "string"
          }
        },
        "required": [
          "action"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "application"
      ],
      "scope": "application",
      "capability": "workspace_mutate",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "action",
        "operations": [
          "list",
          "switch",
          "create",
          "hide",
          "unhide",
          "delete",
          "add_folder",
          "remove_folder",
          "list_tabs",
          "select_tab",
          "create_tab",
          "close_tab"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "application"
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Manage the file selection used by all tools.\n\n**Operations**: get | add | remove | set | clear | preview | promote | demote\n\n**Modes** (how files appear in context):\n- `full` (default): Complete file content\n- `slices`: Specific line ranges only\n- `codemap_only`: API signatures only (function/type definitions)\n\n**Key behaviors**:\n- Incremental context changes use `op=add` / `op=remove`\n- `op=set` with `mode=full`: Complete selection replacement\n- `op=set` with `mode=codemap_only`: Complete codemap-only replacement\n- `op=set` with `mode=slices`: File-scoped slice replacement (requires `#L` ranges or `slices` entries; preserves unrelated full files and slices)\n- Mixed full-file + slice additions use `op=add` with both `paths` and `slices`\n- Auto-codemap: When adding files with `mode=full/slices`, related files get auto-added as codemaps\n- Manual mode: Using `mode=codemap_only`, `promote`, or `demote` disables auto-management\n\n**Path handling**:\n- Accepts files or directories (directories expand recursively)\n- Relative or absolute paths accepted\n- Multi-root: prefix with root name (e.g., \"ProjectA/src/main.swift\")\n- Single-root: prefix optional\n- Fuzzy matching enabled by default\n- Exact `_git_data/...` aliases advertised by the Git tool may be added, removed, set, or previewed as full files\n- Git artifact aliases do not support fuzzy matching, folder expansion, codemaps, or slices\n\n**Options**:\n- `view`: \"summary\" | \"files\" | \"content\" | \"codemaps\" (default: \"summary\")\n- `path_display`: \"relative\" | \"full\" (default: \"relative\")\n- `strict`: When true, errors if no paths resolve (default: false)\n\n**Examples**:\n- Get selection: `{\"op\":\"get\",\"view\":\"files\"}`\n- Add files: `{\"op\":\"add\",\"paths\":[\"src/main.swift\"]}`\n- Add slices: `{\"op\":\"add\",\"slices\":[{\"path\":\"file.swift\",\"ranges\":[{\"start_line\":45,\"end_line\":120}]}]}`\n- Set codemap-only: `{\"op\":\"set\",\"paths\":[\"utils/\"],\"mode\":\"codemap_only\"}`\n- Promote codemap→full: `{\"op\":\"promote\",\"paths\":[\"helper.swift\"]}`\n\nRelated: get_file_tree, file_search, workspace_context, prompt, apply_edits",
      "name": "manage_selection",
      "input_schema": {
        "properties": {
          "mode": {
            "description": "How to represent files in selection: 'full' (complete content), 'slices' (line ranges), or 'codemap_only' (signatures only). With op=set, mode changes semantics (see 'op=set semantics' above).",
            "enum": [
              "full",
              "slices",
              "codemap_only"
            ],
            "type": "string"
          },
          "op": {
            "description": "Operation",
            "enum": [
              "get",
              "add",
              "remove",
              "set",
              "clear",
              "preview",
              "promote",
              "demote"
            ],
            "type": "string"
          },
          "path_display": {
            "description": "Path display for blocks",
            "enum": [
              "full",
              "relative"
            ],
            "type": "string"
          },
          "paths": {
            "description": "File or folder paths (required for add/remove/set)",
            "items": {
              "description": "Relative or absolute file or folder path",
              "type": "string"
            },
            "type": "array"
          },
          "slices": {
            "description": "Selection slices to apply (path + line ranges)",
            "items": {
              "properties": {
                "lines": {
                  "description": "Comma-separated shorthand like '10-20,40'",
                  "type": "string"
                },
                "path": {
                  "description": "Relative or absolute file path",
                  "type": "string"
                },
                "ranges": {
                  "description": "Explicit line ranges (inclusive)",
                  "items": {
                    "properties": {
                      "description": {
                        "description": "Optional slice description (aliases: desc, label)",
                        "type": "string"
                      },
                      "end_line": {
                        "description": "1-based end line",
                        "type": "integer"
                      },
                      "start_line": {
                        "description": "1-based start line",
                        "type": "integer"
                      }
                    },
                    "required": [
                      "start_line"
                    ],
                    "type": "object"
                  },
                  "type": "array"
                }
              },
              "required": [
                "path"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "strict": {
            "description": "Throw when no paths resolve (mutations)",
            "type": "boolean"
          },
          "view": {
            "description": "Amount of detail to return",
            "enum": [
              "summary",
              "files",
              "content",
              "codemaps"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "selection_mutate",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "get",
          "add",
          "remove",
          "set",
          "clear",
          "preview",
          "promote",
          "demote"
        ],
        "aliases": {},
        "default_operation": "get",
        "normalization": "lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "window"
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": true,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Create, delete, or move files.\n\n**Always use absolute paths** for every `path` / `new_path` argument.\n\n**Actions**:\n- `create`: Create file with `content`. New files are auto-selected.\n  - `if_exists`: \"error\" (default) | \"overwrite\"\n- `delete`: Move file or folder to the macOS Trash. Recoverable from Finder Trash until emptied.\n- `move`: Rename/move to `new_path`. Fails if destination exists. Selection state transfers with file.\n\n**Path handling**:\n- Absolute paths only for `path` and `new_path`.\n- Missing parent directories are created automatically.\n\n**Examples**:\n- Create: `{\"action\":\"create\",\"path\":\"/Users/me/project/src/new.swift\",\"content\":\"// code\"}`\n- Overwrite: `{\"action\":\"create\",\"path\":\"/Users/me/project/src/file.swift\",\"content\":\"// new\",\"if_exists\":\"overwrite\"}`\n- Delete: `{\"action\":\"delete\",\"path\":\"/Users/me/project/old.swift\"}` moves the item to Trash.\n- Move: `{\"action\":\"move\",\"path\":\"/Users/me/project/old.swift\",\"new_path\":\"/Users/me/project/renamed.swift\"}`",
      "name": "file_actions",
      "input_schema": {
        "properties": {
          "action": {
            "description": "Operation to perform",
            "enum": [
              "create",
              "delete",
              "move"
            ],
            "type": "string"
          },
          "content": {
            "description": "File content (for create)",
            "type": "string"
          },
          "if_exists": {
            "description": "Behavior if the file already exists (for create)",
            "enum": [
              "error",
              "overwrite"
            ],
            "type": "string"
          },
          "new_path": {
            "description": "New path (for move)",
            "type": "string"
          },
          "operation_id": {
            "description": "Optional caller-stable correlation ID echoed in the mutation acknowledgement; not a deduplication or status lookup key",
            "type": "string"
          },
          "path": {
            "description": "File path",
            "type": "string"
          }
        },
        "required": [
          "action",
          "path"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "file_management",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "action",
        "operations": [
          "create",
          "delete",
          "move"
        ],
        "aliases": {
          "rename": "move"
        },
        "default_operation": null,
        "normalization": "lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "window"
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Return root-local committed code structure for explicit paths or the current selection.\n\n- `paths`: Optional file/directory seeds; omit to use the authoritative current selection.\n- `expand`: Optional `uses`, `used_by`, or `both`; omit for seeds only.\n- `depth`: Relationship depth 1...4 (default 1; meaningful only with `expand`).\n- `signatures`: Include codemap signature text (default true). False performs no artifact demands.\n- `size`: Output size `small`, `medium` (default), or `large`.\n\nInspect per-root results in mixed workspaces. `updates_pending` graph data is usable.\nWhen `truncated` is present, rerun the same call with the next larger `size`.\nSeeds render before related files, so small outputs preserve the graph and degrade signature text gracefully.\n\nExamples:\n- Signatures for a folder: {\"paths\":[\"Sources/Auth/\"]}\n- Who uses a file with large output: {\"paths\":[\"Sources/Auth/SessionStore.swift\"],\"expand\":\"used_by\",\"size\":\"large\"}\n- Cheap graph-only sweep: {\"expand\":\"both\",\"depth\":2,\"signatures\":false,\"size\":\"small\"}",
      "name": "get_code_structure",
      "input_schema": {
        "additionalProperties": false,
        "properties": {
          "depth": {
            "description": "Relationship depth 1...4 (default 1)",
            "type": "integer"
          },
          "expand": {
            "description": "Relationships from each seed's perspective",
            "enum": [
              "uses",
              "used_by",
              "both"
            ],
            "type": "string"
          },
          "paths": {
            "description": "Optional one to 256 file or directory paths; omit for current selection",
            "items": {
              "description": "File or directory path",
              "type": "string"
            },
            "type": "array"
          },
          "signatures": {
            "description": "Include codemap signature text (default true)",
            "type": "boolean"
          },
          "size": {
            "description": "Output size (default medium)",
            "enum": [
              "small",
              "medium",
              "large"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "structural_explore",
      "admission_class": "small_read",
      "operation_policy": null,
      "limits": {
        "connection_lane": 2,
        "resource_lease": 2,
        "resource_scope": "window"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Generate ASCII directory tree of the project.\n\n**Types**:\n- `files` (default): Directory tree with files\n- `roots`: List loaded root folders only\n\n**Modes** (for type=\"files\"):\n- `auto` (default): Full tree, auto-trims depth if too large (~10k token target)\n- `full`: Complete tree (can be very large)\n- `folders`: Directories only, no files\n- `selected`: Only selected files and their parent directories\n\n**Options**:\n- `path`: Start from specific folder (modes/max_depth apply from there)\n- `max_depth`: Limit depth (root=0, immediate children=1, etc.)\n\n**Markers**: `*` = selected file, `+` = has codemap\n\n**Worktree scope**: When an agent session is bound to a Git worktree, displayed paths may remain logical/canonical while filesystem reads use the bound worktree. Responses include `worktree_scope` when this remapping is active.\n\n**Examples**:\n- Auto tree: `{}`\n- Folders only: `{\"mode\":\"folders\"}`\n- Subtree: `{\"path\":\"src/components\",\"max_depth\":2}`\n- Selected files: `{\"mode\":\"selected\"}`",
      "name": "get_file_tree",
      "input_schema": {
        "properties": {
          "max_depth": {
            "description": "Maximum depth (root = 0)",
            "type": "integer"
          },
          "mode": {
            "description": "Filter mode (for 'files' type only, default: 'auto')",
            "enum": [
              "auto",
              "full",
              "folders",
              "selected"
            ],
            "type": "string"
          },
          "path": {
            "description": "Optional starting folder (absolute or relative) when type='files'. When provided, the tree is generated from this folder and 'mode' and 'max_depth' apply from that subtree.",
            "type": "string"
          },
          "type": {
            "description": "Tree type to generate (default: 'files')",
            "enum": [
              "files",
              "roots"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "structural_explore",
      "admission_class": "small_read",
      "operation_policy": null,
      "limits": {
        "connection_lane": 2,
        "resource_lease": 2,
        "resource_scope": "window"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Read file contents with optional line range.\n\n**Parameters**:\n- `path`: File path (required)\n- `start_line`: 1-based line number, or negative for tail behavior\n- `limit`: Number of lines (only with positive start_line)\n\n**Behaviors**:\n- No params: Entire file\n- `start_line=10`: From line 10 to end\n- `start_line=10, limit=20`: Lines 10-29\n- `start_line=-10`: Last 10 lines (like `tail -10`)\n\n**Worktree scope**: When an agent session is bound to a Git worktree, displayed paths may remain logical/canonical while filesystem reads use the bound worktree. Responses include `worktree_scope` when this remapping is active.\n\n**Examples**:\n- Full file: `{\"path\":\"src/main.swift\"}`\n- Lines 50-100: `{\"path\":\"file.swift\",\"start_line\":50,\"limit\":51}`\n- Last 20 lines: `{\"path\":\"file.swift\",\"start_line\":-20}`\n\n**Exact workspace paths**: Existing workspace files use literal-first exact resolution. Reuse a returned path unchanged with `read_file` or `apply_edits`; use `<root-alias>//<relative-path>` when explicit root qualification is needed. Basename, suffix, and head-trim matching are not supported. Approved external read paths remain read-only.",
      "name": "read_file",
      "input_schema": {
        "properties": {
          "limit": {
            "description": "Number of lines to read",
            "type": "integer"
          },
          "path": {
            "description": "File path",
            "type": "string"
          },
          "start_line": {
            "description": "Line to start from (1-based) or negative for tail behavior (-N reads last N lines)",
            "type": "integer"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "file_read",
      "admission_class": "file_read",
      "operation_policy": null,
      "limits": {
        "connection_lane": 0,
        "resource_lease": null,
        "resource_scope": "window"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Search files by path pattern and/or content.\n\n**Modes**:\n- `auto` (default): Detects path vs content search from pattern\n- `path`: Match file paths only (glob-style with regex=false, full regex otherwise)\n- `content`: Search inside file contents\n- `both`: Search paths and contents\n\n**Matching** (regex auto-detected by default):\n- Regex mode: Full regex support (groups, lookarounds, anchors)\n- Literal mode (regex=false): Special chars matched literally, `*`/`?` wildcards for paths\n- Tip: Set `regex=false` to force literal substring matching\n\n**Key options**:\n- `pattern`: Search term (required)\n- `max_results`: Result limit (default: 50)\n- `context_lines`: Lines before/after matches (alias: `-C`)\n- `whole_word`: Match whole words only\n- `count_only`: Return counts only, no content\n- `filter.extensions`: Limit to extensions (e.g., [\".swift\"])\n- `filter.paths`: Limit to paths/folders (can also be a loaded root name like 'RepoPrompt')\n- `filter.exclude`: Skip matching patterns\n\n**Worktree scope**: When an agent session is bound to a Git worktree, displayed paths may remain logical/canonical while filesystem searches use the bound worktree. Responses include `worktree_scope` when this remapping is active.\n\n**Examples**:\n- Literal: `{\"pattern\":\"frame(minWidth:\",\"regex\":false}`\n- Regex OR: `{\"pattern\":\"performSearch|searchUsers\"}`\n- Find files: `{\"pattern\":\"*.swift\",\"mode\":\"path\",\"regex\":false}`\n- With context: `{\"pattern\":\"TODO\",\"context_lines\":2}`\n- Scoped: `{\"pattern\":\"auth\",\"filter\":{\"paths\":[\"src/auth/\"]}}`\n\nResponse capped at ~50k chars; excess results omitted (count reported).",
      "name": "file_search",
      "input_schema": {
        "properties": {
          "context_lines": {
            "description": "Lines of context before/after matches (alias: -C)",
            "type": "integer"
          },
          "count_only": {
            "description": "Return only match count",
            "type": "boolean"
          },
          "filter": {
            "description": "File filtering options (alias: use 'path' string parameter for single-file search)",
            "properties": {
              "exclude": {
                "description": "Skip files/paths matching these patterns",
                "items": {
                  "description": "Pattern like 'node_modules' or '*.log'",
                  "type": "string"
                },
                "type": "array"
              },
              "extensions": {
                "description": "Only search files with these extensions",
                "items": {
                  "description": "File extension like '.js' or '.swift'",
                  "type": "string"
                },
                "type": "array"
              },
              "paths": {
                "description": "Limit search to specific file or folder paths, or a loaded root name",
                "items": {
                  "description": "Absolute path, relative path, or loaded root name (e.g., 'RepoPrompt')",
                  "type": "string"
                },
                "type": "array"
              }
            },
            "type": "object"
          },
          "max_results": {
            "description": "Maximum total results (default: 50)",
            "type": "integer"
          },
          "mode": {
            "description": "Search scope: auto-detects if not specified",
            "enum": [
              "auto",
              "path",
              "content",
              "both"
            ],
            "type": "string"
          },
          "path": {
            "description": "Alias for filter.paths with a single file or folder path",
            "type": "string"
          },
          "pattern": {
            "description": "Search pattern",
            "type": "string"
          },
          "regex": {
            "description": "Use regex matching (default: auto based on pattern)",
            "type": "boolean"
          },
          "whole_word": {
            "description": "Match whole words only",
            "type": "boolean"
          }
        },
        "required": [
          "pattern"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "file_search",
      "admission_class": "file_search",
      "operation_policy": null,
      "limits": {
        "connection_lane": 4,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Canonical workspace context render/export tool.\n\nDefault behavior returns a snapshot of prompt, selection, code structure, and tokens.\nUse `op` for render/export helpers, or omit it for the default snapshot.\n\n**Default includes**: `[\"prompt\",\"selection\",\"code\",\"tokens\"]`\n\n**Available includes**:\n- `prompt`: Current prompt text\n- `selection`: Selected files summary\n- `code`: Code structure (codemaps) for selection\n- `files`: Full file contents\n- `tree`: File tree of selected files\n- `tokens`: Token breakdown by component\n\n**Operations**:\n- `snapshot` (default) — build/render the current workspace context snapshot\n- `export` — write the rendered export to disk\n- `list_presets` — list copy presets\n- `select_preset` — select the active copy preset for the bound tab\n\n**Options**:\n- `include`: Array of sections to include for snapshot rendering\n- `path_display`: \"relative\" | \"full\"\n- `copy_preset`: Override copy preset for token calculation / export rendering\n\n**Worktree scope**: When an agent session is bound to a Git worktree, displayed paths may remain logical/canonical while filesystem reads/searches use the bound worktree. Responses include `worktree_scope` when this remapping is active.\n\n**Examples**:\n- Default snapshot: `{}`\n- With file contents: `{\"include\":[\"prompt\",\"selection\",\"files\"]}`\n- Export: `{\"op\":\"export\",\"path\":\"context.txt\"}`\n- Preset override: `{\"copy_preset\":\"Plan\"}`\n\nRelated: manage_selection, get_file_tree, ask_oracle",
      "name": "workspace_context",
      "input_schema": {
        "properties": {
          "copy_preset": {
            "description": "Preset UUID, kind, or name",
            "type": "string"
          },
          "include": {
            "description": "What to include (defaults to prompt, selection, code, tokens)",
            "items": {
              "enum": [
                "prompt",
                "selection",
                "code",
                "files",
                "tree",
                "tokens"
              ],
              "type": "string"
            },
            "type": "array"
          },
          "op": {
            "description": "Operation (default: 'snapshot')",
            "enum": [
              "snapshot",
              "export",
              "list_presets",
              "select_preset"
            ],
            "type": "string"
          },
          "path": {
            "description": "File path for export operation",
            "type": "string"
          },
          "path_display": {
            "description": "Path display for blocks",
            "enum": [
              "full",
              "relative"
            ],
            "type": "string"
          },
          "preset": {
            "description": "Preset UUID, kind, or name",
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "workspace_read",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "snapshot",
          "export",
          "list_presets",
          "select_preset"
        ],
        "aliases": {},
        "default_operation": "snapshot",
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "window"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Get or modify the shared prompt (instructions/notes).\n\n**Operations**: get | set | append | clear | export | list_presets | select_preset\n\n**Parameters by op**:\n- `set`/`append`: `text` (required)\n- `export`: `path` (required), `copy_preset` (optional override)\n- `select_preset`: `preset` (required) - UUID, kind, or name\n\n**Notes**:\n- `select_preset` requires an explicitly bound tab context (not available during discovery runs)\n- `export` writes clipboard content to file so it can be copy/pasted into ChatGPT (or another AI) for a second opinion; use `copy_preset` to override format\n- `list_presets` returns all available copy presets with configurations\n\n**Examples**:\n- Get: `{\"op\":\"get\"}`\n- Set: `{\"op\":\"set\",\"text\":\"Focus on error handling\"}`\n- Export: `{\"op\":\"export\",\"path\":\"context.txt\"}`\n- List presets: `{\"op\":\"list_presets\"}`\n- Select preset: `{\"op\":\"select_preset\",\"preset\":\"Plan\"}`\n\nRelated: workspace_context, manage_selection, ask_oracle",
      "name": "prompt",
      "input_schema": {
        "properties": {
          "copy_preset": {
            "description": "Preset UUID, kind, or name",
            "type": "string"
          },
          "op": {
            "description": "Operation (default: 'get')",
            "enum": [
              "get",
              "set",
              "append",
              "clear",
              "export",
              "list_presets",
              "select_preset"
            ],
            "type": "string"
          },
          "path": {
            "description": "File path (required for export)",
            "type": "string"
          },
          "preset": {
            "description": "Preset UUID, kind, or name",
            "type": "string"
          },
          "text": {
            "description": "Text for set/append",
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "prompt_mutate",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "get",
          "set",
          "append",
          "clear",
          "export",
          "list_presets",
          "select_preset"
        ],
        "aliases": {},
        "default_operation": "get",
        "normalization": "lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "window"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": true,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Apply direct file edits. Provide exactly ONE of these three modes:\n\n**Mode 1: Rewrite** - Replace entire file content\n`{\"path\": \"file.swift\", \"rewrite\": \"new content...\", \"on_missing\": \"create\"}`\n\n**Mode 2: Single replacement** - Find and replace text\n`{\"path\": \"file.swift\", \"search\": \"oldCode\", \"replace\": \"newCode\", \"all\": true}`\n\n**Mode 3: Multiple edits** - Apply several replacements\n`{\"path\": \"file.swift\", \"edits\": [{\"search\": \"old1\", \"replace\": \"new1\"}, {\"search\": \"old2\", \"replace\": \"new2\"}]}`\n\nNote: Modes are mutually exclusive. Providing more than one will result in an error.\n\nOptions: `verbose` (show diff), `on_missing` (for rewrite only: \"error\" | \"create\", default: \"error\")\nEdits are literal. Use real JSON newlines for multi-line search/replace (not `\\n`). If a match fails, the tool may retry internally with escape decoding.",
      "name": "apply_edits",
      "input_schema": {
        "properties": {
          "all": {
            "description": "Replace all occurrences (default: false)",
            "type": "boolean"
          },
          "edits": {
            "description": "Multiple edits",
            "items": {
              "properties": {
                "all": {
                  "description": "Replace all occurrences (default: false)",
                  "type": "boolean"
                },
                "replace": {
                  "description": "Replacement text",
                  "type": "string"
                },
                "search": {
                  "description": "Text to find",
                  "type": "string"
                }
              },
              "required": [
                "search",
                "replace"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "on_missing": {
            "description": "Behavior when the file is missing (only for `rewrite`)",
            "enum": [
              "error",
              "create"
            ],
            "type": "string"
          },
          "operation_id": {
            "description": "Optional caller-stable correlation ID echoed in applied mutation replies; this does not provide deduplication or replay safety",
            "type": "string"
          },
          "path": {
            "description": "File path",
            "type": "string"
          },
          "replace": {
            "description": "Replacement text",
            "type": "string"
          },
          "rewrite": {
            "description": "Replace the entire file content with this string",
            "type": "string"
          },
          "search": {
            "description": "Text to find",
            "type": "string"
          },
          "verbose": {
            "description": "Include diff preview",
            "type": "boolean"
          }
        },
        "required": [
          "path"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "file_content_edit",
      "admission_class": "exclusive",
      "operation_policy": null,
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "window"
      },
      "shared_read": false
    },
    {
      "annotations": {},
      "description": "Oracle helper utilities.\n\nUse this for read-only oracle-specific helpers:\n- `op=\"models\"`   → list model choices relevant to oracle sends\n- `op=\"sessions\"` → list oracle/chat sessions for the current workspace. Pass context_id to filter to a specific context's sessions.\n\nUse `ask_oracle` for all send/continue turns.",
      "name": "oracle_utils",
      "input_schema": {
        "properties": {
          "context_id": {
            "description": "Context UUID to filter to a specific context's sessions. Use bind_context op=list to discover values.",
            "type": "string"
          },
          "limit": {
            "description": "Maximum sessions to return for the sessions operation",
            "type": "integer"
          },
          "op": {
            "description": "Helper operation",
            "enum": [
              "models",
              "sessions"
            ],
            "type": "string"
          },
          "scope": {
            "description": "Filter scope: 'workspace' (default) or 'tab'. Auto-inferred when context_id is provided.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "conversation_helper",
      "admission_class": "control",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "models",
          "sessions"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Agent-mode oracle send/continue tool.\n\nUse this to start or continue an oracle conversation in `chat`, `plan`, or `review` mode for the current agent tab.\n\nPass `export_response: true` to write the response to a shareable file and get back shareable `oracle_export_path` / `oracle_export_instruction` values. To hand the export to a child agent, include `oracle_export_path` inside the `message` (or `messages`) you send on your next delegation call; your system prompt names the specific delegation tool available to you.\n\nUse `oracle_chat_log` after compaction to recover recent oracle messages.",
      "name": "ask_oracle",
      "input_schema": {
        "properties": {
          "chat_id": {
            "description": "Continue a specific chat in the current agent tab",
            "type": "string"
          },
          "export_response": {
            "description": "When true, export the response to a file and return `oracle_export_path` plus `oracle_export_instruction`. Include `oracle_export_path` inside the `message` you send on your next delegation call; the specific delegation tool is named by your system prompt.",
            "type": "boolean"
          },
          "message": {
            "description": "Your message to send",
            "minLength": 1,
            "type": "string"
          },
          "mode": {
            "default": "chat",
            "description": "Operation mode",
            "enum": [
              "chat",
              "plan",
              "review"
            ],
            "type": "string"
          },
          "new_chat": {
            "description": "Start a new chat session (default: false; discouraged)",
            "type": "boolean"
          }
        },
        "required": [
          "message"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_conversation_send",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Consult a second AI for planning, review, or questions.\n\nUse this to start or continue an oracle conversation in `chat`, `plan`, or `review` mode.\nUse `oracle_utils` for passive helpers like models and sessions.\n\nPass `export_response: true` to write the response to a shareable file and get back shareable `oracle_export_path` / `oracle_export_instruction` values. To hand the export to a child agent, include `oracle_export_path` inside the `message` (or `messages`) you send on your next delegation call; your system prompt names the specific delegation tool available to you.\n\nBuild context first with file reads, `manage_selection`, or `workspace_context`.",
      "name": "oracle_send",
      "input_schema": {
        "properties": {
          "chat_id": {
            "description": "Continue a specific chat in the current tab or current context",
            "type": "string"
          },
          "export_response": {
            "description": "When true, export the response to a file and return `oracle_export_path` plus `oracle_export_instruction`. Include `oracle_export_path` inside the `message` you send on your next delegation call; the specific delegation tool is named by your system prompt.",
            "type": "boolean"
          },
          "message": {
            "description": "Your message to send",
            "minLength": 1,
            "type": "string"
          },
          "mode": {
            "default": "chat",
            "description": "Operation mode",
            "enum": [
              "chat",
              "plan",
              "review"
            ],
            "type": "string"
          },
          "model": {
            "description": "Model preset ID or name override",
            "type": "string"
          },
          "new_chat": {
            "description": "Start a new chat session (default: false; discouraged)",
            "type": "boolean"
          }
        },
        "required": [
          "message"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "conversation_send",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Read recent Oracle conversation messages to recover context during agent mode.\n\nReturns the tail of an Oracle chat as lightweight `{ role, text }` objects. Available only during agent mode runs.\n\n**Parameters**:\n- `chat_id` (optional): Target a specific Oracle chat (short ID or UUID). Omit to read the most recent one.\n- `limit` (optional): Number of messages to return (default: 8, range: 1–50)\n- `include_user` (optional): Include your own messages in output (default: false)",
      "name": "oracle_chat_log",
      "input_schema": {
        "properties": {
          "chat_id": {
            "description": "Chat ID (short ID or UUID) to read",
            "type": "string"
          },
          "include_user": {
            "description": "Include user messages in output (default: false)",
            "type": "boolean"
          },
          "limit": {
            "description": "Max number of messages to return (default: 8, min: 1, max: 50)",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "conversation_log",
      "admission_class": "small_read",
      "operation_policy": null,
      "limits": {
        "connection_lane": 2,
        "resource_lease": 2,
        "resource_scope": "window"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Safe, read-only git operations.\n\n**Operations**: status | diff | log | show | blame\n\n**Compare specs** (for diff/show):\n| Spec | Meaning |\n|------|--------|\n| `uncommitted` | Working dir vs HEAD (default) |\n| `staged` | Staged changes vs HEAD |\n| `unstaged` | Working dir vs staged |\n| `back:N` | HEAD~N..HEAD |\n| `mergebase:X` | Working dir vs merge-base with X |\n| `main` | Working dir vs merge-base with trunk branch (auto-detected) |\n| `uncommitted:main` | Uncommitted vs merge-base with trunk branch |\n| `staged:main` | Staged vs merge-base with trunk branch |\n| `trunk` | Alias for `main` |\n| `last` | vs CURRENT snapshot |\n| `<snapshot_id>` | vs specific snapshot |\n| `<revspec>` | Any git revspec |\n\n**Detail levels** (for diff/show):\n- `summary` (default): Totals only\n- `files`: File list with stats\n- `patches`: Patch hunks, truncated for safety (~300 lines)\n- `full`: Patch hunks, untruncated (may be large)\n\n**Publishing artifacts** (`artifacts=true`):\nWrites snapshot files to disk for persistent reference. **Required for ask_oracle review mode** to include git diff context.\n- Creates MAP.txt, files.tsv, and optional patches\n- Primary review artifacts are auto-selected into context when possible\n- `mode`: \"quick\" | \"standard\" | \"deep\" (default: \"standard\")\n- `scope`: \"all\" | \"selected\" — filter to selected files only\n\n**Repo targeting**:\n- Generic calls default to the first loaded root's repo; nested Agent Context Builder runs default to their frozen selected repository target\n- `repo_root`: Target specific repo (path or name)\n- `repo_roots`: Array for multi-repo operations (status, diff)\n- Tree specifiers: append `@wt` (explicit worktree), `@main` (main checkout), or `@main:<branch>` to target a worktree by branch (local branch name)\n\n**Safety**: --no-ext-diff, --no-textconv, --color=never, GIT_TERMINAL_PROMPT=0\n\n**Examples**:\n- Status: `{\"op\":\"status\"}`\n- Main checkout status: `{\"op\":\"status\",\"repo_root\":\"@main\"}`\n- Worktree by branch: `{\"op\":\"status\",\"repo_root\":\"@main:main\"}`\n- Diff vs trunk: `{\"op\":\"diff\",\"compare\":\"main\"}`\n- Quick diff: `{\"op\":\"diff\",\"detail\":\"files\"}`\n- Inline patches: `{\"op\":\"diff\",\"detail\":\"patches\"}`\n- Full untruncated diff: `{\"op\":\"diff\",\"detail\":\"full\"}`\n- Publish for review: `{\"op\":\"diff\",\"artifacts\":true,\"scope\":\"selected\"}`\n- Recent commits: `{\"op\":\"log\",\"count\":5}`\n\nNote: log/show/blame run on primary repo only with multi-root.",
      "name": "git",
      "input_schema": {
        "properties": {
          "artifacts": {
            "description": "Write snapshot artifacts (diff only); primary review artifacts are auto-selected into context when possible",
            "type": "boolean"
          },
          "compare": {
            "description": "Compare spec for diff/show (supports main/trunk aliases)",
            "type": "string"
          },
          "context_lines": {
            "description": "Diff context lines",
            "type": "integer"
          },
          "count": {
            "description": "Number of commits for log",
            "type": "integer"
          },
          "detail": {
            "description": "Detail level for diff/show",
            "enum": [
              "summary",
              "files",
              "patches",
              "full"
            ],
            "type": "string"
          },
          "detect_renames": {
            "description": "Enable rename detection",
            "type": "boolean"
          },
          "inline": {
            "properties": {
              "map": {
                "description": "Include MAP excerpt",
                "type": "boolean"
              },
              "max_lines": {
                "description": "Max MAP lines",
                "type": "integer"
              },
              "mode": {
                "description": "Inline mode",
                "enum": [
                  "brief",
                  "full"
                ],
                "type": "string"
              }
            },
            "type": "object"
          },
          "lines": {
            "description": "Line range for blame (e.g., \"45-60\")",
            "type": "string"
          },
          "mode": {
            "description": "Artifact mode for diff",
            "enum": [
              "quick",
              "standard",
              "deep"
            ],
            "type": "string"
          },
          "op": {
            "description": "Operation",
            "enum": [
              "status",
              "diff",
              "log",
              "show",
              "blame"
            ],
            "type": "string"
          },
          "path": {
            "description": "Single pathspec",
            "type": "string"
          },
          "paths": {
            "description": "Multiple pathspecs",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "ref": {
            "description": "Ref for show operation",
            "type": "string"
          },
          "repo_key": {
            "description": "Repository key (optional alternative to repo_root)",
            "type": "string"
          },
          "repo_root": {
            "description": "Repository root path inside a loaded root, or loaded root name. Generic calls default to the first loaded root; nested Agent Context Builder runs use the frozen selected repository target. Supports @wt, @main, or @main:<branch> to target a worktree by branch (local branch name).",
            "type": "string"
          },
          "repo_roots": {
            "description": "Multiple repository root paths inside loaded roots, or root names (for multi-root operations). Supports @wt, @main, or @main:<branch> suffixes.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "scope": {
            "description": "Diff scope",
            "enum": [
              "all",
              "selected"
            ],
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "git_read",
      "admission_class": "git_read",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "status",
          "diff",
          "log",
          "show",
          "blame"
        ],
        "aliases": {},
        "default_operation": "status",
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 2,
        "resource_lease": 1,
        "resource_scope": "repository"
      },
      "shared_read": true
    },
    {
      "annotations": {
        "destructiveHint": true,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Manage Git worktrees, per-agent-session worktree bindings, and session-bound worktree merges.\n\n**Management ops**: list | show | create | bind | select | unbind\n**Merge ops**: preview | apply | status | continue | abort\n\n**Selectors**:\n- `repo_root`: Optional loaded root path/name, git-style specifier such as `@main`, or merge source-binding disambiguator.\n- `worktree`: Worktree selector (`@current`, `@main`, `@branch:<name>`, name, branch, path, or `@id:<worktree_id>`).\n- `worktree_id`: Durable worktree ID alternative to `worktree`.\n- `target`: Merge preview target selector; defaults to `@main`.\n- `target_worktree_id`: Durable merge target ID alternative to `target`.\n\n**Session binding and merge source**:\n- `bind` and `select` persist a binding for one Agent session.\n- Merge ops use the Agent session's bound source worktree; `repo_root` disambiguates when multiple bindings exist.\n- `session_id` is optional only when MCP routing resolves an active Agent session; otherwise provide it explicitly.\n- `create` can also bind with `bind=true`; `unbind` removes the selected root binding, or all bindings with `all=true`.\n\n**Merge safety**:\n- `preview` is non-mutating and publishes bounded artifacts by default.\n- `apply` requires `operation_id`; plain MCP callers must pass `confirm_preview=true`.\n- Routed Agent Mode apply calls without confirmation request user approval before mutation.\n- `continue` and `abort` require `confirm=true` outside routed UI flows.\n\n**Visual identity**:\n- `label`, `color`, `icon_name`, and `marker_style` are serialized with worktree identity on create/bind.\n- `color` must be `#RRGGBB`; `marker_style` is `dot`, `ring`, or `capsule`.\n\n**Output**:\n- Management op JSON includes repository/worktree IDs, visual identity, bindings, previous_binding on replacement, and graph placeholders.\n- Merge op JSON keeps merge details under the nested `merge` block.\n- Formatted output is compact and stable for humans.",
      "name": "manage_worktree",
      "input_schema": {
        "properties": {
          "all": {
            "description": "Unbind: remove all worktree bindings for the session.",
            "type": "boolean"
          },
          "allow_external_path": {
            "description": "Create: allow explicit paths outside RepoPrompt's app-managed worktree container.",
            "type": "boolean"
          },
          "base_ref": {
            "description": "Create: optional base ref/commit for the new worktree.",
            "type": "string"
          },
          "bind": {
            "description": "Create: bind the created worktree to a target Agent session.",
            "type": "boolean"
          },
          "branch": {
            "description": "Create: branch name to create/check out.",
            "type": "string"
          },
          "color": {
            "description": "Create/bind: visual color as #RRGGBB.",
            "type": "string"
          },
          "commit_message": {
            "description": "Merge apply/continue: optional merge commit message.",
            "type": "string"
          },
          "confirm": {
            "description": "Merge continue/abort: plain MCP confirmation. Required true outside routed UI flows.",
            "type": "boolean"
          },
          "confirm_preview": {
            "description": "Merge apply: plain MCP confirmation. Required true outside routed Agent Mode approval flows.",
            "type": "boolean"
          },
          "context_lines": {
            "description": "Merge preview: diff artifact context lines. Default 3; clamped to 0...20.",
            "type": "integer"
          },
          "detach": {
            "description": "Create: create a detached worktree.",
            "type": "boolean"
          },
          "detect_renames": {
            "description": "Merge preview: detect renames in preview artifacts. Default false.",
            "type": "boolean"
          },
          "force": {
            "description": "Create: pass --force to git worktree add.",
            "type": "boolean"
          },
          "graph_limit": {
            "description": "Graph/visualization line cap. Default 24; clamped to 1...200.",
            "type": "integer"
          },
          "icon_name": {
            "description": "Create/bind: SF Symbol name for future UI display.",
            "type": "string"
          },
          "include_graph": {
            "description": "Include bounded graph/visualization metadata. Merge ops default true; list/show default false.",
            "type": "boolean"
          },
          "include_status": {
            "description": "Include a compact dirty summary for each returned worktree. Default false.",
            "type": "boolean"
          },
          "label": {
            "description": "Create/bind: visual label to persist for this worktree.",
            "type": "string"
          },
          "marker_style": {
            "description": "Create/bind: visual marker style",
            "enum": [
              "dot",
              "ring",
              "capsule"
            ],
            "type": "string"
          },
          "op": {
            "description": "Operation",
            "enum": [
              "list",
              "show",
              "create",
              "bind",
              "select",
              "unbind",
              "preview",
              "apply",
              "status",
              "continue",
              "abort"
            ],
            "type": "string"
          },
          "operation_id": {
            "description": "Merge apply/status/continue/abort: operation ID returned by preview.",
            "type": "string"
          },
          "path": {
            "description": "Create: explicit absolute worktree path. External paths require allow_external_path=true.",
            "type": "string"
          },
          "persist_visuals": {
            "description": "For list/show, persist fallback visual identities instead of returning deterministic fallbacks only.",
            "type": "boolean"
          },
          "publish_artifacts": {
            "description": "Merge preview: publish preview artifacts. Default true.",
            "type": "boolean"
          },
          "repo_key": {
            "description": "Optional repository key alternative to repo_root.",
            "type": "string"
          },
          "repo_root": {
            "description": "Optional loaded root path/name or repo/worktree specifier. Defaults to the first loaded Git repo.",
            "type": "string"
          },
          "session_id": {
            "description": "Target Agent session for bind/select/unbind, or create with bind=true.",
            "type": "string"
          },
          "target": {
            "description": "Merge preview: target worktree selector. Defaults to @main.",
            "type": "string"
          },
          "target_worktree_id": {
            "description": "Merge preview: target worktree ID alternative to target.",
            "type": "string"
          },
          "worktree": {
            "description": "Worktree selector: @current, @main, @branch:<name>, branch/name/path, or @id:<worktree_id>.",
            "type": "string"
          },
          "worktree_id": {
            "description": "Durable worktree ID alternative to worktree. Mutually exclusive with worktree.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "worktree_manage",
      "admission_class": "exclusive",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "list",
          "show",
          "create",
          "bind",
          "select",
          "unbind",
          "preview",
          "apply",
          "status",
          "continue",
          "abort"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 1,
        "resource_lease": 1,
        "resource_scope": "window"
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Intelligently explore the codebase and build optimal file context for a task.\n\nA Context Builder agent analyzes your codebase, selects relevant files within a token budget, and rewrites your instructions into a clarified prompt. Describe **what** you need, not **where** to look — the agent discovers the right files autonomously. Mention what you know and what you're unsure about; being too prescriptive narrows discovery.\n\n**response_type** (what happens after context building):\n| Type | Behavior |\n|------|----------|\n| (omit) or `clarify` | Context only — returns selection and prompt for you to use |\n| `question` | Answers a question about the codebase using built context |\n| `plan` | Generates implementation plan for the task |\n| `review` | Generates code review with git diff context |\n\n**Structuring instructions** (XML tags):\n- `<task>`: Main goal\n- `<context>`: Background, constraints, known file references\n- `<discovery_agent-guidelines>`: Optional starting hints for the agent (not passed to follow-up model). The agent explores beyond these freely — omit if you don't have specific leads.\n\n**Example**:\n```\n<task>Add user authentication using JWT</task>\n<context>The app has an existing session system. See docs/auth-spec.md for requirements.</context>\n<discovery_agent-guidelines>There may be auth-related code in src/auth/ already</discovery_agent-guidelines>\n```\n\n**Exporting**: Pass `export_response: true` (requires a `response_type` that generates a response) to write the result to a file and get back `oracle_export_path` plus `oracle_export_instruction`. To hand the export to a child agent, include `oracle_export_path` inside the `message` (or `messages`) you send on your next delegation call; your system prompt names the specific delegation tool available to you.\n\n**Workflow**: Continue with `ask_oracle(chat_id: \"<returned_id>\", new_chat: false)`. Refine with `manage_selection`.\n\n**Agent mode behavior**: If this tool is invoked during an Agent Mode run, it reuses the current agent tab instead of creating a new tab.\n\n**Timing**: 30s-5min depending on codebase size and task complexity.",
      "name": "context_builder",
      "input_schema": {
        "properties": {
          "export_response": {
            "description": "When true, export the generated response to a file and return `oracle_export_path` plus `oracle_export_instruction`. Requires a response_type that generates a response. Include `oracle_export_path` inside the `message` you send on your next delegation call; the specific delegation tool is named by your system prompt.",
            "type": "boolean"
          },
          "instructions": {
            "description": "Your request, ideally structured with XML tags: <task> for the main goal, <context> for background/constraints/file references, <discovery_agent-guidelines> for optional starting hints. Describe what you need — the agent finds the right files.",
            "type": "string"
          },
          "response_type": {
            "description": "Optional: 'plan' to generate implementation plan, 'question' to ask a question, or 'review' to generate a code review. Omit or 'clarify' to just return context.",
            "enum": [
              "plan",
              "question",
              "review",
              "clarify"
            ],
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "discovery",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Ask the user a clarifying question and wait for their response.\n\nUse this tool to gather additional context or clarification from the user.\nThe tool will block until the user responds.\n\n**When to use:**\n- Task requirements are ambiguous\n- Multiple valid approaches exist and you need user preference\n- Critical context is missing\n- Confirming before making significant changes\n\n**Best practices:**\n- Ask early, not at the end\n- Be specific - explain what you're trying to determine\n- Provide options when the choices are clear\n- Limit questions to avoid disrupting the user\n\n**Input:**\n- `questions`: Required array of structured questions. Each question requires stable `id` and `question` fields. Use `allows_multiple` and `allows_custom` for selection/custom-answer behavior.\n- `title`: Optional title for the wizard card.\n- `context`: Optional overall context shown above the questions.\n- `timeout_seconds`: Optional timeout in seconds for the whole interaction. Defaults to the global Question Timeout preference.\n\n**Response:**\n- `answers`: Object keyed by question ID. Each value contains `answers`, `selected_options`, `custom_response`, and `skipped`.\n- `timed_out`: True if the interaction timed out.\n- `skipped`: True if user explicitly skipped the interaction.\n- `elapsed_seconds`: How long the user took to respond.",
      "name": "ask_user",
      "input_schema": {
        "properties": {
          "context": {
            "description": "Optional overall context shown above the wizard.",
            "type": "string"
          },
          "questions": {
            "description": "One or more structured questions to ask as a single wizard.",
            "items": {
              "properties": {
                "allows_custom": {
                  "description": "When true, the user can type one custom response. Default is true.",
                  "type": "boolean"
                },
                "allows_multiple": {
                  "description": "When true, the user can select multiple options. Default is false.",
                  "type": "boolean"
                },
                "context": {
                  "description": "Optional per-question context.",
                  "type": "string"
                },
                "header": {
                  "description": "Optional short heading for this question.",
                  "type": "string"
                },
                "id": {
                  "description": "Stable unique question ID used as the response key.",
                  "type": "string"
                },
                "options": {
                  "description": "Optional suggested answers. Each entry may be a string label or an object with label/description.",
                  "items": {
                    "anyOf": [
                      {
                        "description": "Option label returned when selected.",
                        "type": "string"
                      },
                      {
                        "properties": {
                          "description": {
                            "description": "Optional option description shown to the user.",
                            "type": "string"
                          },
                          "label": {
                            "description": "Option label returned when selected.",
                            "type": "string"
                          }
                        },
                        "required": [
                          "label"
                        ],
                        "type": "object"
                      }
                    ]
                  },
                  "type": "array"
                },
                "question": {
                  "description": "Question text to show the user.",
                  "type": "string"
                }
              },
              "required": [
                "id",
                "question"
              ],
              "type": "object"
            },
            "type": "array"
          },
          "timeout_seconds": {
            "description": "Timeout in seconds for the whole interaction. Defaults to the global Question Timeout preference.",
            "type": "integer"
          },
          "title": {
            "description": "Optional title shown above the question wizard.",
            "type": "string"
          }
        },
        "required": [
          "questions"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "user_interaction",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Short-lived, read-only explore child agents for narrow codebase probes. Each child runs in a fresh session with its own context window. Always uses the `explore` role; no custom `model_id`, workflows, session reuse, `steer`, or `respond`.\n\nExplore children inherit the caller's worktree bindings by default; pass `inherit_worktree=false` to opt out. Start-only worktree controls can bind an existing worktree or create one before provider startup, overriding an inherited primary-root binding. Multi-message creates produce one worktree per child when branch/path are implicit and reject a shared explicit branch or path.\n\n**Operations**: start | poll | wait | cancel\n\n- `start`: Launch one or more fresh explore sessions. Provide `message` for one probe or `messages` for multiple probes. Batch starts wait for the first referenced session to finish or need input unless `detach=true`.\n- `poll`: Return current snapshot immediately for `session_id` or `session_ids`.\n- `wait`: Block until the first referenced explore run finishes or needs input. `timeout=0` behaves like poll.\n- `cancel`: Cancel a live explore child session.\n\nExplore children are read-only — no edits, oracle calls, or further sub-agent spawning.",
      "name": "agent_explore",
      "input_schema": {
        "description": "Provide `op` plus operation-specific fields.\n\n**start**: message or messages (required, mutually exclusive), detach?, timeout?, inherit_worktree?, worktree|worktree_id|worktree_create? and worktree_* args\n**poll / wait**: session_id or session_ids (mutually exclusive), timeout? (wait only)\n**cancel**: session_id (required)",
        "properties": {
          "allow_external_worktree_path": {
            "description": "[start + worktree_create] Allow explicit worktree_path outside RepoPrompt's app-managed worktree container.",
            "type": "boolean"
          },
          "detach": {
            "description": "[start] Return immediately instead of waiting. Default false.",
            "type": "boolean"
          },
          "inherit_worktree": {
            "description": "[start] When started from an Agent Mode run, inherit the source session's worktree bindings before provider startup. Default true. Set false to keep parent session threading but skip worktree inheritance; explicit worktree/worktree_id/worktree_create args still bind the requested worktree.",
            "type": "boolean"
          },
          "message": {
            "description": "[start] Exploration instruction text for one fresh explore child. Mutually exclusive with messages.",
            "type": "string"
          },
          "messages": {
            "description": "[start] Array of exploration instruction strings. Mutually exclusive with message. Starts one fresh explore child per entry.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "op": {
            "description": "Operation.",
            "enum": [
              "start",
              "poll",
              "wait",
              "cancel"
            ],
            "type": "string"
          },
          "session_id": {
            "description": "[poll, wait, cancel] Explore child session UUID returned by start.",
            "type": "string"
          },
          "session_ids": {
            "description": "[wait, poll] Array of explore child session UUIDs. Mutually exclusive with session_id.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "timeout": {
            "description": "[start, wait] Max wait seconds. 0 = poll. Default 120.",
            "type": "number"
          },
          "worktree": {
            "description": "[start] Existing worktree selector to bind before provider startup: @current, @main, @branch:<name>, name, branch, path, or @id:<worktree_id>. Mutually exclusive with worktree_id and worktree_create.",
            "type": "string"
          },
          "worktree_base_ref": {
            "description": "[start + worktree_create] Optional base ref/commit for the new worktree.",
            "type": "string"
          },
          "worktree_branch": {
            "description": "[start + worktree_create] Optional branch name for the new worktree. Defaults to an rp/agent/<session>-... branch.",
            "type": "string"
          },
          "worktree_color": {
            "description": "[start] Optional visual color to persist for the bound worktree as #RRGGBB.",
            "type": "string"
          },
          "worktree_create": {
            "description": "[start] Create an app-managed Git worktree, bind it to the new session, materialize its hidden root, then start the provider. Mutually exclusive with worktree/worktree_id.",
            "type": "boolean"
          },
          "worktree_id": {
            "description": "[start] Durable worktree ID to bind before provider startup. Mutually exclusive with worktree and worktree_create.",
            "type": "string"
          },
          "worktree_label": {
            "description": "[start] Optional visual label to persist for the bound worktree.",
            "type": "string"
          },
          "worktree_path": {
            "description": "[start + worktree_create] Optional explicit absolute path (or ~/...). External paths require allow_external_worktree_path=true.",
            "type": "string"
          },
          "worktree_repo_root": {
            "description": "[start] Repo/logical root selector for worktree resolution or creation. Defaults to the declared primary workspace root.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_explore_control",
      "admission_class": "control",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "start",
          "poll",
          "wait",
          "cancel"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Spawn and control Agent Mode sessions. `start` always creates a new session/tab; use `steer` to continue an existing session.\n\n**Role labels** — pass as `model_id` to select via the global role-default mapping:\n- `explore` — Fast exploration and codebase mapping\n- `engineer` — Balanced engineering work\n- `pair` — Interactive pair programming with highest-tier models\n- `design` — Architecture, design discussions, creative problem solving; writes a markdown review document (saved under `docs/reviews/`, `docs/designs/`, or `docs/analysis/`) as its primary deliverable for review/analysis tasks\n\nRole labels resolve through the effective global role-default mapping; see the top-level `task_labels` array from `agent_manage.list_agents` for the authoritative label→model mapping. If `model_id` is omitted on `start`, RepoPrompt uses the `pair` role. To pin an exact agent+model+effort target, pass a specific compound `model_id` from `agents[].models[].model_id` in the same response.\n\n**Operations**: start | poll | wait | cancel | steer | respond | attach | detach\n\n- `start`: Launch an agent run in a **new** session/tab. Do NOT pass `session_id` — use `steer` to continue an existing session. Omit `model_id` to use the `pair` role, or pass `model_id` with a role label (resolved via the global role-default mapping in `agent_manage.list_agents` `task_labels`) or an explicit compound `model_id` from `agents[].models[].model_id`. When started from an Agent Mode run, the new child session inherits the source session's worktree bindings by default; pass `inherit_worktree=false` to keep parent session threading but skip worktree inheritance. Optional start-only worktree args can bind the new session to an existing worktree (`worktree`/`worktree_id`) or create an app-managed worktree (`worktree_create=true`) before provider startup; explicit worktree args take precedence, suppress parent inheritance, and bind only the requested worktree. Returns a `session_id` — save it for all follow-up calls. Waits up to `timeout` seconds (default 120). Pass `detach: true` to return immediately.\n- `poll`: Return current snapshot immediately. Accepts `session_id` (single) or `session_ids` (array — returns all current snapshots).\n- `wait`: Block until the run finishes or needs input. Default 120s. `timeout: 0` = poll. Accepts `session_id` (single) or `session_ids` (array — returns when first session reaches interesting state). Returns `interaction_id` when input is pending. A steering interruption may include `wait.steering_message` as caller context; it does not acknowledge provider delivery or instruct the caller to resend.\n- `cancel`: Stop an active agent run. Only valid when the run is `running` or `waiting_for_input`. Requires `session_id`.\n- `steer`: Continue an existing agent session by sending a follow-up instruction to the `session_id` returned by `start`. If the run is still active, the instruction is steered into that run; if the last run already finished or the MCP wait/control handle expired, RepoPrompt reactivates the existing Agent session and starts the next run in the same session when it still exists. Pass `wait: true` (or `timeout_seconds`) to block until the steered run finishes or needs input. Do NOT use `steer` when status is `waiting_for_input` — use `respond` instead.\n- `respond`: Resolve the current pending interaction. Requires `session_id` and the exact `interaction_id` from the latest snapshot. For approvals, send the advertised choice in the top-level scalar `response` field, for example `response=\"accept\"`. For MCP elicitation, use `response` (`accept`, `decline`, or `cancel`) plus optional object `content` and `meta`.\n- `attach`: Join a host-owned session as a second client (same `session_id` the GUI uses). Returns a wait-compatible snapshot plus `replay`, `delivery_cursor`, and `generation`. Optional `resume_cursor` / `resume_generation` continue the event stream. After attach, `wait` / `poll` / `respond` work on this handle.\n- `detach`: Unsubscribe this MCP handle from the host session. Execution continues; use `attach` to rejoin.\n\n**session_id lifecycle**: `start` creates a new session and returns `session_id` in the response. All subsequent operations on that run require passing the same `session_id` back. Do NOT invent session IDs — always use the value returned by `start`.\n\n**Sub-agent spawning**: MCP-started `orchestrate` runs can dispatch sub-agents. Sub-agents cannot recursively start additional agent runs.\n\n**Parallel agents**: When launching multiple agents in parallel, always use `detach: true` so each `start` returns immediately without blocking. You can then `wait` or `poll` each `session_id` independently.\n\n**IMPORTANT — never end your turn with active agents**: Sub-agents may need approval for tool calls or ask questions via `waiting_for_input`. Always `wait`/`poll` on every started session and `respond` to any pending interactions before finishing your turn. An unattended agent will stall indefinitely.",
      "name": "agent_run",
      "input_schema": {
        "description": "Provide `op` plus operation-specific fields.\n\n**start**: message (required), model_id? (defaults to pair), session_name?, workflow_id|workflow_name?, detach?, timeout?, inherit_worktree?, worktree|worktree_id|worktree_create? and worktree_* args. Use workflow_name=\"orchestrate\" to plan, decompose, and dispatch sub-agents.\n**poll / wait**: session_id or session_ids (mutually exclusive), timeout? (wait only)\n**cancel**: session_id (required)\n**steer**: session_id (required, from a prior `start`/`steer` response), message (required), wait?, timeout_seconds?, workflow_id|workflow_name?\n**respond**: session_id (required), interaction_id (required), response? (top-level scalar string; approval example: response=\"accept\"), answers?, amendment?, content?, meta?\n**attach**: session_id (required), resume_cursor?, resume_generation?\n**detach**: session_id (required)",
        "properties": {
          "_worktree_startup_benchmark_token": {
            "description": "[DEBUG start] Single-use token from the scoped worktree startup benchmark diagnostics surface.",
            "type": "string"
          },
          "allow_external_worktree_path": {
            "description": "[start + worktree_create] Allow explicit worktree_path outside RepoPrompt's app-managed worktree container.",
            "type": "boolean"
          },
          "amendment": {
            "description": "[respond] Amendment text for accept_with_amendment decisions.",
            "type": "string"
          },
          "answers": {
            "description": "[respond] Structured answers keyed by question ID.",
            "type": "object"
          },
          "content": {
            "description": "[respond] MCP elicitation content object to send with action=accept.",
            "type": "object"
          },
          "detach": {
            "description": "[start] Return immediately instead of waiting. Default false.",
            "type": "boolean"
          },
          "inherit_worktree": {
            "description": "[start] When started from an Agent Mode run, inherit the source session's worktree bindings before provider startup. Default true. Set false to keep parent session threading but skip worktree inheritance. Explicit worktree/worktree_id/worktree_create args take precedence, suppress parent inheritance, and bind only the requested worktree.",
            "type": "boolean"
          },
          "interaction_id": {
            "description": "[respond] Pending interaction UUID from the snapshot. Returned as a top-level field in poll/wait responses when the run is waiting_for_input.",
            "type": "string"
          },
          "message": {
            "description": "[start, steer] Instruction text. Required for start and steer. If sharing an exported plan, include the path/instruction directly in this text.",
            "type": "string"
          },
          "meta": {
            "description": "[respond] Optional MCP elicitation _meta object.",
            "type": "object"
          },
          "model_id": {
            "description": "[start] Role label from agent_manage.list_agents task_labels (explore, engineer, pair, design — resolved via global role defaults), or an explicit compound model_id from agents[].models[].model_id to pin an exact target. Defaults to pair when omitted.",
            "type": "string"
          },
          "op": {
            "description": "Operation.",
            "enum": [
              "start",
              "poll",
              "wait",
              "cancel",
              "steer",
              "respond",
              "attach",
              "detach"
            ],
            "type": "string"
          },
          "response": {
            "description": "[respond] Canonical top-level scalar string. For approvals, pass one advertised response option, for example response=\"accept\"; decision and nested response objects are unsupported. For instructions and questions, pass response text. For MCP elicitation, pass accept, decline, or cancel; a non-action string is sent as content.response.",
            "type": "string"
          },
          "session_id": {
            "description": "[poll, wait, cancel, steer, respond, attach, detach] Session UUID returned by a prior start/steer/attach response. Do not fabricate it. Not accepted by start — use steer to continue or attach to join an existing host session.",
            "type": "string"
          },
          "resume_cursor": {
            "description": "[attach] Last delivered cursor to resume from. Omit for a full snapshot.",
            "type": "number"
          },
          "resume_generation": {
            "description": "[attach] Hex generation token from a prior attach. Pair with resume_cursor.",
            "type": "string"
          },
          "session_ids": {
            "description": "[wait, poll] Array of session UUIDs. For wait: returns when first session reaches interesting state. For poll: returns all current snapshots. Mutually exclusive with session_id.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "session_name": {
            "description": "[start] Display name for a new session.",
            "type": "string"
          },
          "timeout": {
            "description": "[start, wait] Max wait seconds. 0 = poll. Default 120.",
            "type": "number"
          },
          "timeout_seconds": {
            "description": "[steer] Max wait seconds when wait=true. 0 = immediate post-steer snapshot. Default 120.",
            "type": "number"
          },
          "wait": {
            "description": "[steer] Wait for an interesting/terminal state after steering. Implied when timeout_seconds is provided.",
            "type": "boolean"
          },
          "workflow_id": {
            "description": "[start, steer, respond] Workflow ID. Mutually exclusive with workflow_name.",
            "type": "string"
          },
          "workflow_name": {
            "description": "[start, steer, respond] Workflow name. Mutually exclusive with workflow_id.",
            "type": "string"
          },
          "worktree": {
            "description": "[start] Existing worktree selector to bind before provider startup: @current, @main, @branch:<name>, name, branch, path, or @id:<worktree_id>. Mutually exclusive with worktree_id and worktree_create.",
            "type": "string"
          },
          "worktree_base_ref": {
            "description": "[start + worktree_create] Optional base ref/commit for the new worktree.",
            "type": "string"
          },
          "worktree_branch": {
            "description": "[start + worktree_create] Optional branch name for the new worktree. Defaults to an rp/agent/<session>-... branch.",
            "type": "string"
          },
          "worktree_color": {
            "description": "[start] Optional visual color to persist for the bound worktree as #RRGGBB.",
            "type": "string"
          },
          "worktree_create": {
            "description": "[start] Create an app-managed Git worktree, bind it to the new session, materialize its hidden root, then start the provider. Mutually exclusive with worktree/worktree_id.",
            "type": "boolean"
          },
          "worktree_id": {
            "description": "[start] Durable worktree ID to bind before provider startup. Mutually exclusive with worktree and worktree_create.",
            "type": "string"
          },
          "worktree_label": {
            "description": "[start] Optional visual label to persist for the bound worktree.",
            "type": "string"
          },
          "worktree_path": {
            "description": "[start + worktree_create] Optional explicit absolute path (or ~/...). External paths require allow_external_worktree_path=true.",
            "type": "string"
          },
          "worktree_repo_root": {
            "description": "[start] Repo/logical root selector for worktree resolution or creation. Defaults to the declared primary workspace root.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_external_control",
      "admission_class": "control",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "start",
          "poll",
          "wait",
          "cancel",
          "steer",
          "respond",
          "attach",
          "detach"
        ],
        "aliases": {},
        "default_operation": "wait",
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "List agents, manage sessions, and browse workflows.\n\n**Operations**: list_agents | list_sessions | get_log | extract_handoff | handoff | create_session | resume_session | stop_session | cleanup_sessions | list_workflows\n\n- `list_agents`: Returns top-level `task_labels` as the authoritative role-label→model mapping (explore, engineer, pair, design), plus `agents[].models[]` with explicit compound `model_id` targets for callers that want to pin a specific agent/model/effort. Use `task_labels` entries for role-based routing; use `agents[].models[].model_id` for exact selections. Pass `roles_only=true` to return only `task_labels` and omit the explicit per-agent target catalog.\n- `list_sessions`: Browse sessions. Returns `session_id` for each session. Filter by MCP-facing `state` (e.g. `running`, `waiting_for_input`, `completed`, `failed`). When called from agent mode, automatically scopes to sessions spawned by the current agent session.\n- `get_log`: Read faithful transcript XML for a session, preserving visible assistant/tool order without handoff compaction or narration pruning. Use `offset`/`limit` to page by turns.\n- `extract_handoff` (`handoff` alias): Export the full `<forked_session ...>` handoff XML for a live or persisted session. Persisted sessions export transcript-only payloads; `include_file_contents` is accepted only for a live source tab that is currently active so file selection can be snapshotted reliably. Use `output_path` to write to a file; inline XML is returned by default only when no output path is provided.\n- `create_session` / `resume_session`: Create or resume a session with a specific `model_id`.\n- `stop_session`: Stop a live session.\n- `cleanup_sessions`: Delete up to 256 specific MCP-originated sessions by ID. The entire array must contain unique valid UUID strings; any non-string, invalid UUID, or duplicate rejects the request before lookup or mutation. Only sessions started via MCP are eligible; user-created sessions are never deleted. Skips active sessions. Cancellation before mutation returns the current and remaining IDs as unprocessed/retry IDs. Cancellation after mutation starts but before durable deletion reports the current ID as retryable `mutation_cancelled`, returns only later IDs as unprocessed/retry, and stops the batch. Cancellation after durable deletion keeps the current ID in `deleted_sessions` with `durable=true`, leaves it out of retry IDs, returns only later IDs as unprocessed/retry, and stops the batch. Per-ID lookup and persisted-session load failures are `resolution_failed`. Durable deletion failures preserve live UI/session state and are `delete_failed`; open-tab failures include `durable=false` and `local_cleanup_completed=false`. Missing or previously deleted IDs are `already_absent` and do not make an otherwise successful response partial. Use `list_sessions` first to find session IDs, then pass them here.\n- `list_workflows`: Discover workflows usable with `agent_run` operations, including `orchestrate` for planning, decomposition, and sub-agent dispatch.",
      "name": "agent_manage",
      "input_schema": {
        "description": "Provide `op` plus operation-specific fields.\n\n**list_agents**: roles_only?\n**list_workflows**: no additional fields\n**list_sessions**: agent?, state?, limit?\n**get_log**: session_id (required), offset?, limit?\n**extract_handoff / handoff**: session_id (required), up_to_item_id?, include_file_contents?, output_path?, overwrite?, inline?, max_transcript_items?, max_tool_args_characters?\n**create_session**: model_id?, session_name?\n**resume_session**: session_id (required), model_id?\n**stop_session**: session_id (required)\n**cleanup_sessions**: session_ids (required, array of 1...256 session UUIDs)\n\nDefault extraction behavior: `extract_handoff` (or alias `handoff`) returns `handoff_xml` inline when `output_path` is omitted. When `output_path` is provided, XML is written to disk and omitted from the response unless `inline=true`. `output_path` must be absolute (or `~/...`); CLI shorthand resolves relative paths before calling MCP.",
        "properties": {
          "include_file_contents": {
            "description": "[extract_handoff] Include file contents only when the source session is live and its tab is active. Default false.",
            "type": "boolean"
          },
          "inline": {
            "description": "[extract_handoff] Include handoff_xml in the response. Default true without output_path, false with output_path.",
            "type": "boolean"
          },
          "limit": {
            "description": "[list_sessions, get_log] Max results.",
            "type": "integer"
          },
          "max_tool_args_characters": {
            "description": "[extract_handoff] Tool argument character budget; clamped to 0...20000. Default 2000.",
            "type": "integer"
          },
          "max_transcript_items": {
            "description": "[extract_handoff] Transcript item budget; clamped to 1...1000. Default 200.",
            "type": "integer"
          },
          "model_id": {
            "description": "[create_session, resume_session] Role label from list_agents task_labels (explore, engineer, pair, design — resolved via global role defaults), or an explicit compound model_id from list_agents agents[].models[].model_id.",
            "type": "string"
          },
          "offset": {
            "description": "[get_log] Turn offset.",
            "type": "integer"
          },
          "op": {
            "description": "Operation.",
            "enum": [
              "list_agents",
              "list_sessions",
              "get_log",
              "extract_handoff",
              "handoff",
              "create_session",
              "resume_session",
              "stop_session",
              "cleanup_sessions",
              "list_workflows"
            ],
            "type": "string"
          },
          "output_path": {
            "description": "[extract_handoff] Absolute output path (or ~/...) for the handoff XML. When set, inline XML is omitted unless inline=true.",
            "type": "string"
          },
          "overwrite": {
            "description": "[extract_handoff] Whether output_path may replace an existing file. Default true.",
            "type": "boolean"
          },
          "roles_only": {
            "description": "[list_agents] When true, return only the authoritative role-label mapping (task_labels) and omit the explicit per-agent target catalog. Default false.",
            "type": "boolean"
          },
          "session_id": {
            "description": "[get_log, extract_handoff, resume_session, stop_session] Session UUID.",
            "type": "string"
          },
          "session_ids": {
            "description": "[cleanup_sessions] Array of 1...256 unique valid session UUID strings. Any non-string, invalid UUID, or duplicate rejects the entire request before lookup or mutation.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "session_name": {
            "description": "[create_session] Display name for a new session.",
            "type": "string"
          },
          "state": {
            "description": "[list_sessions] Session state filter. Use MCP-facing values such as running, waiting_for_input, completed, failed.",
            "type": "string"
          },
          "up_to_item_id": {
            "description": "[extract_handoff] Optional transcript row UUID cutoff.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_external_control",
      "admission_class": "control",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "list_agents",
          "list_sessions",
          "get_log",
          "extract_handoff",
          "create_session",
          "resume_session",
          "stop_session",
          "cleanup_sessions",
          "list_workflows"
        ],
        "aliases": {
          "handoff": "extract_handoff"
        },
        "default_operation": "list_sessions",
        "normalization": "trimmed_lowercased"
      },
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Share real-time progress updates with the user.\n\n**Critical**: This is the PRIMARY way to provide live feedback during operations.\nWithout this tool, users see nothing until you call `wait_for_next_user_instruction` -\nthey're left staring at a loading state wondering what's happening.\n\nUse this tool PROACTIVELY to narrate your progress as you work:\n- \"Looking for authentication-related files...\"\n- \"Found UserService.swift, reading to understand the pattern...\"\n- \"Making changes to the login flow...\"\n\n**When to use (frequently!):**\n- Exploring a codebase (searching, reading multiple files)\n- Working through multi-step implementations\n- Any task taking more than a few seconds\n- Before and after significant operations\n\n**Notes:**\n- Messages appear with a \"thinking\" indicator\n- Use the optional `title` parameter for categorization (e.g., \"Searching\", \"Analyzing\", \"Planning\")",
      "name": "share_thoughts",
      "input_schema": {
        "properties": {
          "thoughts": {
            "description": "Your thoughts or reasoning to share with the user.",
            "type": "string"
          },
          "title": {
            "description": "Optional short title for the thought (e.g., 'Analyzing', 'Planning').",
            "type": "string"
          }
        },
        "required": [
          "thoughts"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_reasoning_control",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Rename the current agent session/tab.\n\nUse this tool near session start to set a helpful session title.",
      "name": "set_status",
      "input_schema": {
        "properties": {
          "session_name": {
            "description": "Optional session/tab title to set for the active session tab.",
            "type": "string"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_session_control",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "openWorldHint": false,
        "readOnlyHint": false
      },
      "description": "Complete your turn and receive the user's next message.\n\n**CRITICAL - YOU MUST ALWAYS CALL THIS TOOL**\nThis is how you deliver your response to the user. Without calling this tool, the user sees NOTHING and the session hangs. You must call this after EVERY turn - whether you completed a task, answered a question, or just want to share information.\n\n**How it works:**\n- The `prompt` you provide IS your message to the user - make it your complete response\n- After you call this, you receive the user's reply as your next turn (like a normal conversation)\n- The wait defaults to 600 seconds and honors a caller-supplied `timeout_seconds`; a timeout returns `timed_out: true`\n- Do NOT send a separate text response before calling this tool - the prompt IS your response\n\n**Writing your response (the `prompt` parameter):**\n- Be verbose and thorough - explain what you did, what you found, or what you're thinking\n- Write naturally as if speaking to a colleague - no need to end with a question\n- Include relevant details: files changed, code snippets, reasoning, observations\n- Example: \"I've refactored the authentication module to use JWT tokens. The changes include:\n\n1. **TokenManager.swift** - New class handling token generation and validation\n2. **AuthMiddleware.swift** - Updated to use TokenManager instead of session-based auth\n3. **UserController.swift** - Login endpoint now returns JWT in response body\n\nAll existing tests pass, and I added new tests for token expiration handling.\"\n- DO NOT write terse responses like \"Done.\" - be informative and helpful\n\n**The user's response comes as your next turn:**\n- After calling this tool, you'll receive the user's message as input to your next turn\n- This is just like a normal conversation - no special handling needed\n- You don't need to ask \"what's next?\" - just present your response naturally",
      "name": "wait_for_next_user_instruction",
      "input_schema": {
        "properties": {
          "prompt": {
            "description": "Your response to the user - what you want to say before waiting for their next instruction.",
            "type": "string"
          },
          "timeout_seconds": {
            "description": "Optional wait timeout in seconds. Default 600.",
            "type": "integer"
          }
        },
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "agent_reasoning_control",
      "admission_class": "control",
      "operation_policy": null,
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": false
    },
    {
      "annotations": {
        "destructiveHint": false,
        "idempotentHint": true,
        "openWorldHint": false,
        "readOnlyHint": true
      },
      "description": "Query past Agent Mode session transcripts across all workspaces. All operations are read-only.\n\n**Operations**: list_sessions | search | time | get_session\n\n- `list_sessions`: Session inventory with content-aware filters (workspace, agent kind, model, files touched, date range). Returns session metadata including duration, turn count, and files touched.\n- `search`: Full-text search across session transcripts and summaries. Matches against both live activity text and compacted turn summaries. Returns snippets with ~200 chars of context around each match.\n- `get_session`: Read a bounded, noise-reduced window around a known session turn. Use `search` first, then call `get_session` with `session_id`, `around_turn`, `context_turns: 0`, and a modest `max_chars` for a single search hit; whole-session dumps are intentionally unsupported.\n- `time`: Aggregate time-in-session analytics. Groups by day, week, month, session, or workspace. Active duration uses the settings-backed default idle threshold (currently 10 minutes) unless `idle_threshold_minutes` is provided.\n\n**Scope**: Window routing does not imply workspace scope; history scans all saved workspaces by default. Use `workspace` to filter by saved name, UUID, or `Workspace-*` storage directory. Stale indexes are skipped and reported in `skipped_workspaces`.\n\n**Truncation**: `truncated` means `limit` capped returned results; `scan_truncated` means `max_sessions_scanned` or a cooperative workspace/index/byte/turn/elapsed budget capped work. `scan_diagnostics` identifies budget limits with typed counters and `retryable`; narrow `workspace`, `session_id`, or date scope before retrying.\n**Caching**: The cross-workspace session inventory is cached for ~90 seconds for query-loop performance. A session saved within that window may not appear in `list_sessions`/`search`/`time` until the cache expires. `get_session` first resolves the exact session filename without a full inventory scan, then retains one cache-bypassed fresh-scan fallback for just-saved sessions; transcript content is signature-checked and cache-invalidated when the session file changes.",
      "name": "history",
      "input_schema": {
        "description": "Provide `op` plus operation-specific fields.\n\n**list_sessions**: workspace?, agent_kind?, model?, touched_file?, date_from?, date_to?, sort?, limit?, idle_threshold_minutes?, max_sessions_scanned?\n**search**: query (required), workspace?, session_id?, source?, date_from?, date_to?, limit?, max_sessions_scanned?, include_turn_request_text?\n**get_session**: session_id (required), around_turn? + context_turns?, or turn_start? + turn_end?, roles?, max_chars?\n**time**: group_by (required), workspace?, session_id?, date_from?, date_to?, limit?, include_details?, idle_threshold_minutes?, max_sessions_scanned?",
        "properties": {
          "agent_kind": {
            "description": "[list_sessions] Agent kind filter (e.g. claudeCodeGLM, codexExec, acp).",
            "type": "string"
          },
          "around_turn": {
            "description": "[get_session] Turn index to inspect, usually copied from a search result. Returns a small window around this turn.",
            "type": "integer"
          },
          "context_turns": {
            "description": "[get_session] Number of turns before/after around_turn. Use 0 for the cheapest target-turn-only follow-up to a search result. Default 1, max 5.",
            "type": "integer"
          },
          "date_from": {
            "description": "ISO 8601 lower date bound (e.g. 2026-01-01T00:00:00Z).",
            "type": "string"
          },
          "date_to": {
            "description": "ISO 8601 upper date bound.",
            "type": "string"
          },
          "group_by": {
            "description": "[time] Grouping dimension (required for time).",
            "enum": [
              "day",
              "week",
              "month",
              "session",
              "workspace"
            ],
            "type": "string"
          },
          "idle_threshold_minutes": {
            "description": "[list_sessions, time] Idle gap threshold in minutes for active duration. Omitted uses the app setting/default (currently 10). Range 0...1440.",
            "type": "integer"
          },
          "include_details": {
            "description": "[time] Verbose opt-in: include per-session breakdowns in each group. Default false.",
            "type": "boolean"
          },
          "include_turn_request_text": {
            "description": "[search] Verbose opt-in: include clipped matched-turn user request text. Default false to keep output compact.",
            "type": "boolean"
          },
          "limit": {
            "description": "Max returned results. list_sessions default 30, search default 20, max 100.",
            "type": "integer"
          },
          "max_chars": {
            "description": "[get_session] Hard cap on returned text. Default 6000, max 20000.",
            "type": "integer"
          },
          "max_sessions_scanned": {
            "description": "[list_sessions, search, time] Max sessions hydrated/scanned before scan_truncated. Default 200, cap 1000. Independent cooperative inventory/turn/elapsed budgets may also truncate and are reported in scan_diagnostics.",
            "type": "integer"
          },
          "model": {
            "description": "[list_sessions] Model substring match.",
            "type": "string"
          },
          "op": {
            "description": "Operation.",
            "enum": [
              "list_sessions",
              "search",
              "time",
              "get_session"
            ],
            "type": "string"
          },
          "query": {
            "description": "[search] Search term (required for search). Case-insensitive substring match.",
            "type": "string"
          },
          "roles": {
            "description": "[get_session] Included roles. Default: user, assistant, errors, summaries. Tool calls are summarized per turn; include role `tool` only when individual tool entries are needed.",
            "items": {
              "description": "[get_session] Role to include",
              "enum": [
                "user",
                "assistant",
                "tool",
                "error",
                "summary",
                "system",
                "thinking"
              ],
              "type": "string"
            },
            "type": "array"
          },
          "session_id": {
            "description": "[search, time, get_session] Limit to a specific session UUID.",
            "type": "string"
          },
          "sort": {
            "description": "[list_sessions] Sort order: last_activity (default), duration, turn_count.",
            "enum": [
              "last_activity",
              "duration",
              "turn_count"
            ],
            "type": "string"
          },
          "source": {
            "description": "[search] Where to search: activities, summaries, or all (default all).",
            "enum": [
              "activities",
              "summaries",
              "all"
            ],
            "type": "string"
          },
          "touched_file": {
            "description": "[list_sessions] Filter sessions that edited or read this file path.",
            "type": "string"
          },
          "turn_end": {
            "description": "[get_session] Inclusive end turn for a bounded range. Max returned span is 20 turns.",
            "type": "integer"
          },
          "turn_start": {
            "description": "[get_session] Inclusive start turn for a bounded range. Requires no around_turn. Max returned span is 20 turns.",
            "type": "integer"
          },
          "workspace": {
            "description": "Limit to saved workspace name, UUID, or Workspace-* storage directory.",
            "type": "string"
          }
        },
        "required": [
          "op"
        ],
        "type": "object"
      },
      "enabled_by_default": true,
      "registration_scopes": [
        "window",
        "standalone"
      ],
      "scope": "window",
      "capability": "history_read",
      "admission_class": "control",
      "operation_policy": {
        "argument_key": "op",
        "operations": [
          "list_sessions",
          "search",
          "time",
          "get_session"
        ],
        "aliases": {},
        "default_operation": null,
        "normalization": "exact"
      },
      "limits": {
        "connection_lane": 8,
        "resource_lease": null,
        "resource_scope": null
      },
      "shared_read": true
    }
  ]
}
